-- File to make and test queries
SELECT * FROM company_db.departments;
SELECT id, title, salary, department_id FROM company_db.roles;
SELECT first_name, last_name, role_id, manager_id FROM company_db.employees;

-- Query for Add department
